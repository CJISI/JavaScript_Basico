let altura_cm = 171;
let altura_m = 1.71;
let peso = 85.0;

let altura = Math.round(altura_m)
console.log(altura)
let peso_red = Math.floor(peso)
console.log(peso_red)

let max_valor = (Number.MAX_VALUE + 1) === Number.MAX_VALUE;
console.log(max_valor)
